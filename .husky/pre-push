#!/bin/sh
set -e

BRANCH=$(git rev-parse --abbrev-ref HEAD)

if [ "$BRANCH" = "main" ] || [ "$BRANCH" = "develop" ]; then
  echo ""
  echo "✖ Direct push to $BRANCH is not allowed."
  echo "  Open a PR from a feature branch."
  echo ""
  exit 1
fi

echo ""
echo "Running strict pre-push quality gates..."
echo ""

npm run lint
npm run typecheck
npm run test:ci
npm run build
npx start-server-and-test "npm start" http-get://127.0.0.1:4200 "npm run e2e:ci"
npm run test:integration -- --workers=1

echo ""
echo "✔ All strict quality gates passed."
echo ""
